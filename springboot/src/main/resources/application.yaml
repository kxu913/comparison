server:
  port: 8080
spring:
  application:
    name: springboot
  datasource:
    url: jdbc:postgresql://localhost:5432/feed?sslmode=disable
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 50